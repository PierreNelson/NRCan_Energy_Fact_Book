# NRCan Energy Factbook Data Pipeline Dependencies

# Data processing
pandas>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
xlrd>=2.0.0
pip-system-certs

# Database
pyodbc>=5.0.0

# Configuration
pyyaml>=6.0.0
python-dotenv>=1.0.0

# SharePoint / Microsoft Graph (manual Excel sync from BenchScope)
msgraph-sdk>=1.0.0
azure-identity>=1.15.0
# On Windows without long-path support, pip install msgraph-sdk may fail; run:
#   python tools/install_msgraph_sdk.py

# Note: For SQL Server connectivity, you also need:
# - SQL Server (Developer Edition is free)
# - ODBC Driver 17 or 18 for SQL Server
#   Download from: https://docs.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server